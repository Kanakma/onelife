#include <iostream>
using namespace std;
int main(){
  int n, k;
  cin>>n>>k;
  int sum=0;
  int counter=0;
  int a[n];
  for(int i=1; i<=n; i++){
    a[i]=i;
  }
  for(int i=n; i>0; i--){
      if(sum<k){
        sum+=a[j];
      }
      if(sum>k){
      }
      if(sum==k){
        counter++;
        break;
      }
  }
  cout<<counter;
return 0;
}
